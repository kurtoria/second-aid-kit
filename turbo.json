{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".svelte-kit/**", ".vercel/**"]
    },
    "lint": {
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false
    },
    "pack": {
      "cache": false
    }
  }
}
